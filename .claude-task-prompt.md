You are refactoring the Ad Studio pipeline for speed and reliability. Complete ALL of the following changes. Read every file you touch carefully — do not break imports, types, or exports.

## 1. DELETE OPENROUTER COMPLETELY
- Delete src/lib/ai/openrouter-client.ts entirely
- In src/lib/adstudio/ai-providers.ts: remove createOpenRouterTextProvider, createOpenRouterImageProvider, OPENROUTER_CHAT_URL, OPENROUTER_IMAGE_URL, and all openrouter branches in createTextProviderForCandidate/createImageProviderForCandidate
- In src/lib/config/env.ts: remove OPENROUTER_API_KEY from REQUIRED_ENV_KEYS
- In src/lib/ai/model-registry.ts: remove any openrouter provider entries
- In src/lib/ai/model-control-config.ts: remove openrouter references
- In src/lib/ai/model-profile-store.ts: remove openrouter references
- In src/components/model-control-panel.tsx: remove openrouter UI
- In src/lib/operator/assistant.ts: remove openrouter references
- In src/lib/pwa/sw-policy.ts: remove openrouter URL
- In src/lib/workforce/permissions.ts: remove openrouter references
- In src/lib/alerts/paid-service-watchdog.ts: remove openrouter references
- In src/app/(operator)/model-control/page.tsx: remove openrouter references
- In src/app/api/model-profiles/[key]/test/route.ts: remove openrouter references
- In src/lib/adstudio/copy-generation.ts: update error message to not mention OPENROUTER_API_KEY
- Search the entire src/ for any remaining openrouter or OPENROUTER references and remove them all

## 2. DELETE ALL QA (verification, warnings, copy-checking, defect scanning)
The user has their own QA. Remove ALL of it EXCEPT a minimal region-only detection that finds editor hit-boxes.

- In src/lib/adstudio/clone-qa.ts: GUT the file. Remove runCloneQa copy verification (parseCopyChecks, copyChecks, defects, cloneQaPassed logic). Replace with a function detectCloneRegions() that ONLY calls the vision model asking for regions (boxes + keys + kind) and returns them. No copy verification, no defect scanning, no pass/fail. Keep parseCloneRegions and boxFromRegionEntry. Keep applyDeterministicTextEditQa (it is used for instant text-edit feedback). Remove normalizeRenderedText if unused. Remove cloneQaWarnings re-export.
- Delete src/lib/adstudio/creative-qa.ts entirely
- Delete src/lib/adstudio/layout-qa.ts entirely
- Delete src/lib/adstudio/clone-qa-warnings.ts entirely
- In src/lib/adstudio/generate-template-campaign.ts: Remove enrichCloneCreativesWithQa function and the enrichQa return. Instead, after persist, fire-and-forget a background call to detectCloneRegions and write regions onto the creative canvas_json. The main pipeline returns immediately without waiting for regions. Remove CloneQaEnrichmentInput type. Update RunTemplateCampaignGenerationResult to not return enrichQa.
- In src/app/api/adstudio/creatives/[id]/edit/route.ts: Remove the runCloneQa call after edits. For text edits, use applyDeterministicTextEditQa. For image edits, carry forward previous cloneQa. Remove the QA call entirely — the edit returns as soon as the image is composited and persisted.
- In src/components/adstudio/ad-studio-workbench.tsx: Remove the editorPreparing logic and the cloneQa polling loop. The editor should show immediately with whatever regions exist. If no regions yet, show the image without edit overlay (no spinner). Poll for regions in the background but NEVER show a blocking spinner.
- In src/components/adstudio/canvas/in-place-ad-editor.tsx: Remove the Preparing your editor spinner. If regions are empty, just show the image. Editing unlocks when regions arrive (background poll), no spinner.
- In src/components/adstudio/canvas/creative-edit-client.ts: Make qa optional in the response handling — do not error if qa is missing.
- Remove all imports of deleted files. Search for any remaining imports of creative-qa, layout-qa, clone-qa-warnings.

## 3. PROVIDER HEALTH CHECK AT AD STUDIO OPEN
- Create src/app/api/adstudio/health/route.ts: A GET endpoint that checks provider health. For OpenAI: send a 1-token chat completion to verify the key works. For Google: same. Return JSON with openai ok/error, google ok/error, imageProvider ok/error. Timeout each check at 8 seconds. Never throw — return ok:false with error message.
- In the Ad Studio page component (find the page that renders ad-studio-workbench), add a useEffect on mount that fetches /api/adstudio/health. Store result in state. If any provider is down, show a non-blocking yellow banner at the top: Heads up: [provider] is unavailable — some features may be slower. Never block the UI.

## 4. GPT 5.5 FASTEST THINKING
- In src/lib/adstudio/ai-providers.ts, in the postChatCompletion function (or wherever the chat body is built): add reasoning_effort: minimal to the request body when the model is gpt-5.5 or starts with gpt-5. This makes it think as little as possible.

## 5. SECTION-ONLY EDITS (crop region, edit crop, composite back)
- In src/app/api/adstudio/creatives/[id]/edit/route.ts: Instead of sending the full image + mask to the model, CROP the selected region (with about 15 percent padding on each side, clamped to image bounds) from currentImage using sharp. Send only the cropped region to the image edit endpoint. Then composite the edited crop back onto the original full image at the region position. This drastically reduces the pixels the model processes.
- The crop/composite should use sharp (already a dependency). Create helper functions cropRegionWithPadding() and compositeRegionBack() in a new file src/lib/adstudio/region-edit.ts.
- Update buildTargetedEditRequest usage to work with the cropped image instead of full image.

## 6. NO HARD TIMEOUTS — SLOW OVER SILENT FAILURE
- In src/lib/adstudio/providers.ts: Remove any AbortSignal.timeout() or hardcoded timeouts on provider fetch calls. Let them run until they complete or fail naturally.
- In src/app/api/adstudio/creatives/[id]/edit/route.ts: Keep maxDuration = 300 (Vercel needs it) but ensure errors are ALWAYS returned to the client as visible error messages, never swallowed.
- In src/app/api/adstudio/campaigns/route.ts: Same — errors must surface to the client.
- In src/components/adstudio/ad-studio-workbench.tsx and in-place-ad-editor.tsx: If a generation/edit takes more than 60s, show Still working… this can take a minute text. Never show an infinite spinner with no feedback.

## 7. GET TRIGGER.DEV WORKING
- Create src/trigger/adstudio-generate.ts: Define the trigger task adstudio.generate.template that calls runTemplateCampaignGeneration. Use the @trigger.dev/sdk task() API. The task payload should match what triggerTemplateGeneration() in campaigns/route.ts sends: workspaceId, userId, jobId, origin, body.
- Create trigger.config.ts at the project root with the project ref from TRIGGER_PROJECT_ID env var.
- Check package.json for @trigger.dev/sdk — if not installed, note it needs: npm install @trigger.dev/sdk
- The task should: create a Supabase service client, call runTemplateCampaignGeneration, then fire the background region detection. Update the job status in the DB.

## 8. FEED + STORY INDEPENDENT PIPELINES
- In src/lib/adstudio/generate-template-campaign.ts: The runClonePersistencePipeline already uses Promise.all for both formats. Change it so that PRIMARY (4:5 feed) persists and returns to the caller as soon as IT is done, without waiting for STORY (9:16).
- Modify the pipeline so Feed is returned immediately after its own generate-normalize-upload-persist chain completes. Story continues in the background and persists independently.
- The campaign pack should be built with Feed first. Story gets added to the existing campaign when it finishes (update the campaign row).

## 9. BACKGROUND IMAGE SCALING DURING COPY ENTRY
- In src/components/adstudio/new-ad-dialog.tsx (or wherever image upload/scaling happens): When the user selects an image, immediately show it in the slot (raw or thumbnail). Start the scaling/resizing in a Web Worker or async operation. Store the scaled result in a ref/state. When the user hits Generate, use the pre-scaled image if ready; if not, await it (it should be nearly done by then).
- The scaling logic is likely in src/components/adstudio/media-upload.ts — check there.

## 10. FULL COPY EDITOR IN THE TEMPLATE POPUP
- Rebuild the template selection popup (new-ad-dialog.tsx) as a two-step flow:
  - STEP 1 Copy: Show all on-image text fields from the template + feed copy fields (headline, primary text, description, CTA). Three modes via tabs or toggle:
    a) Write my own — empty editable text fields with character limits
    b) AI from brief — a brief textarea + Write copy button that calls the existing /api/adstudio/copy endpoint, then populates the fields with the AI result for editing
    c) Use template sample — pre-filled with the template sample values, all editable
  - STEP 2 Generate: Shows a summary of the copy + image slots. Generate Ad button. Copy is FINAL — no server-side copy generation during image gen.
- Update the generation pipeline: when copy is provided in the request body (firstAd.onImageCopy + firstAd.copy), skip generateAdStudioTemplateCopy entirely and use the provided copy directly. The 25-47s copy generation is eliminated from the critical path.
- The existing /api/adstudio/copy route already supports mode=brief and mode=assist — reuse it for the AI-assist button in the popup.

## CRITICAL RULES
- Do NOT break TypeScript compilation. After all changes, run: npx tsc --noEmit 2>&1 | head -80 and fix any type errors.
- Do NOT touch files outside src/ related to Ad Radar, Leads, or mockups.
- Preserve all existing exports that other files depend on (check imports before deleting).
- Use sharp for image operations (already in package.json).
- Keep the code clean — remove dead imports, unused types, orphaned functions.
- When in doubt, prefer keeping a function as a no-op stub over deleting it if many files import it.

Start by reading the key files, then make changes systematically. Verify with tsc at the end.
