# 2026-09-09

## Ad Builder Recovery & Deploy
- Root cause: `adbuilder` and `property_check` access rules still required `workspaceMode === "ad_studio"`. After unifying to ad studio, users with legacy "monitor" mode workspaces were locked out of ad-builder (403 → error boundary).
- Fixed `src/lib/auth/access-control.ts`: removed `workspaceMode` restriction from `adbuilder` and `property_check` surfaces; allowed all roles including viewer.
- Updated left menu (`src/config/niche/blockwise.ts`): renamed top-level "Ads" → "Ad Builder", grouped sub-items under "Library" section to avoid duplicate "Ads" label.
- Built and deployed new image `blockwise-app:6233fe8bc12a318bf0aa364455c60ad0a3bebe0a`.
- Live revision confirmed at https://blockwise.sale/api/health.
- Also included earlier uncommitted changes: shell unification (deleted monitor/operator), SSO buttons, login copy fixes, trial skeleton.
