{
  "name": "@blockwise/ad-deterministic-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical deterministic renderer for AdStudio TemplatePacks. Exact 1080×1350 (Feed) and 1080×1920 (Story) PNG output. Same inputs → byte-identical PNG hashes. Used by Frank QA and Blockwise Save/Publish. Source-only package: consumers compile src/ directly.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test src/renderer.test.ts"
  },
  "dependencies": {
    "@blockwise/ad-template-pack-contract": "file:../ad-template-pack-contract",
    "@napi-rs/canvas": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
