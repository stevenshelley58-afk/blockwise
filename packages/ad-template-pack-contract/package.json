{
  "name": "@blockwise/ad-template-pack-contract",
  "version": "1.0.0",
  "private": true,
  "description": "Immutable TemplatePack contract shared between Frank (factory) and Blockwise (consumer). Frozen per Phase 2 gate — no field additions without a new schema version. Source-only package: consumers compile src/ directly (Blockwise root tsc + Frank workspace).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test src/contract.test.ts"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
