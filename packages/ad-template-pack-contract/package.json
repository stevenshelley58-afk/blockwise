{
  "name": "@blockwise/ad-template-pack-contract",
  "version": "1.0.0",
  "private": false,
  "description": "Immutable TemplatePack contract shared between Frank (factory) and Blockwise (consumer). Frozen per Phase 2 gate — no field additions without a new schema version.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "tsx": "^4.19.0"
  }
}
