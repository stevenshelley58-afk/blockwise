[Unit]
Description=Retire old Blockwise release worktrees
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
Environment=HOME=/root
ExecStart=/usr/local/sbin/blockwise-prune-current-release --keep 10 --apply
